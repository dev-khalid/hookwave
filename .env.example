AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
SQS_ENDPOINT=
SQS_QUEUE_NAME=webhook-events

# Producer
PRODUCE_INTERVAL=1s   # how often to emit an event (e.g. 500ms, 2s)
PRODUCE_BURST=        # if set, send this many messages then exit (e.g. 1000)
